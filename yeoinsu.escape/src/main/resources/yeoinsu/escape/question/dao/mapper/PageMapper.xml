<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mtbatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="yeoinsu.escape.question.dao.mapper.PageMapper">
	<select id="getTotRowCnt" resultType="int">
		select count(que_no) from questions
	</select>
</mapper>